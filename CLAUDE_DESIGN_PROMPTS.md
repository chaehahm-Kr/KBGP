# Claude Design 운영 가이드 & 프롬프트 세트

K Select Network / K-Beauty Growth Program

---

## PART 1. 구체적으로 줄까, 여유를 줄까

결론부터: **구체적으로 줘야 한다. 단, 구체적으로 줄 대상이 정해져 있다.**

Claude Design은 제약이 없으면 학습된 평균값으로 수렴한다. Inter 폰트, 보라색 그라디언트, 중앙 정렬 3단 카드 — 소위 AI 슬롭이다. 반대로 모든 것을 지정하면 도구가 낼 수 있는 구성적 제안을 스스로 막게 된다.

경계선은 이렇게 긋는다.

| 반드시 못 박을 것 (제약) | 반드시 열어둘 것 (실행) |
|---|---|
| 대상 독자와 그가 두려워하는 것 | 섹션 순서와 구성 |
| 톤 (형용사가 아니라 금지 목록으로) | 그리드 분할 방식, 비대칭 배치 |
| 정확한 HEX 값 | 여백 리듬 |
| 폰트 이름과 웨이트 | 어떤 정보를 시각화할지의 선택 |
| 한글 조판 수치 (자간·행간) | 모션 연출 아이디어 |
| 넣을 실제 문구와 숫자 | 컴포넌트의 세부 형태 |
| 하지 말 것 목록 | 페이지 간 전환 처리 |

핵심 기법 세 가지.

**1) 부정형이 아니라 대안형으로 지정한다.**
"크림색 배경 쓰지 마" → 도구는 크림색을 회피하려다 엉뚱한 곳으로 간다.
"`#FAF9F6` 웜 오프화이트를 쓰고, 순백과 쿨그레이는 쓰지 않는다" → 정확히 착지한다.
DESIGN.md의 컬러 표에 HEX를 다 박아둔 이유다.

**2) 경쟁사 URL이 아니라 문화적 사조를 참조시킨다.**
"HUE&CRY 사이트처럼" 은 도구가 그 사이트를 볼 수 없으므로 추측으로 흐른다.
"스위스 편집 디자인 그리드 + 초대형 그로테스크 타이포 + 단일 액센트" 는 학습된 개념이라 정확히 재현된다.
레퍼런스 이미지는 **첨부**하되, 말로는 사조로 설명한다.

**3) 첫 프롬프트의 밀도가 결과를 결정한다.**
한 번에 밀도 높게 던진 브리프가 성공률이 압도적으로 높다. 짧게 던지고 대화로 고쳐나가는 방식은 평균값에 갇힌 초안을 붙잡고 씨름하게 만든다. 첫 프롬프트에 대상·톤·비주얼 방향·타이포·컬러·콘텐츠를 다 넣는다.

---

## PART 2. 운영 순서

**Step 0 — 프로젝트 생성**
claude.ai/design에서 새 프로젝트. 이름: `K Select Network`.

**Step 1 — DESIGN.md 첨부**
온보딩에서 자산을 올릴 때 `DESIGN.md`를 첨부한다. 스크린샷 여러 장을 흩뿌리는 것보다 정리된 문서 하나가 훨씬 정확하게 작동한다. 이 문서가 프로젝트의 헌법이 된다.

**Step 2 — 레퍼런스 이미지 첨부**
좋아하는 4장을 함께 올린다. 단 캡션을 붙인다: "이 4장에서 가져올 것은 (a) 초대형 타이포가 유일한 비주얼이라는 점, (b) 헤드라인 위 극도로 자간이 넓은 마이크로 라벨, (c) 플로팅 pill 내비게이션, (d) 단일 액센트 색. **가져오지 않을 것**은 마젠타/핑크 컬러와 파스텔 제품 사진이다."

캡션 없이 이미지만 올리면 도구는 색까지 베낀다.

**Step 3 — 첫 프롬프트 (PART 3)로 홈 3안 생성**
처음부터 선형으로 고치지 말고 **3가지 변형**을 받는다.

**Step 4 — 하나를 고르고 나머지에서 좋은 부분만 이식**
"2안을 기준으로 하되, 1안의 절차 레일과 3안의 지표 블록을 가져와라."

**Step 5 — 페이지 확장 (PART 4)**
홈이 확정된 뒤 페이지를 하나씩. 여러 페이지를 한 번에 요청하면 품질이 균일하게 떨어진다.

**Step 6 — Tweaks 패널로 미세 조정**
타이포 크기·간격·색·섹션 순서 슬라이더는 **채팅 토큰을 쓰지 않는다.** 밀도 조절, 섹션 순서 변경, 폰트 사이즈 미세 조정은 전부 여기서 한다. 채팅은 구조와 개념 변경에만 쓴다.

**Step 7 — 인라인 코멘트로 컴포넌트 단위 수정**
"이 카드만 고쳐" 같은 국소 수정은 캔버스에서 해당 요소에 코멘트를 남긴다. 채팅으로 말하면 도구가 페이지 전체를 다시 건드린다.

**Step 8 — Handoff to Claude Code**
Export → "Handoff to Claude Code". 스크린샷을 다시 해석하는 게 아니라 디자인 의도와 구조 결정을 컨텍스트째로 넘긴다. 여기서 Next.js + Tailwind로 구현하고 Git → Vercel로 배포한다.

**토큰 절약 팁:** 수정할 때 새 메시지를 보내는 대신 **이전 메시지를 편집**한다. 대화 히스토리가 짧게 유지되어 품질과 비용이 둘 다 좋아진다.

---

## PART 3. 첫 프롬프트 (그대로 복사)

> 첨부한 DESIGN.md를 이 프로젝트의 기준 문서로 삼아라. 아래 요청과 DESIGN.md가 충돌하면 DESIGN.md를 따른다.
>
> **만들 것:** K Select Network의 홈페이지(데스크톱 + 모바일). K Select Network는 Letusto Inc.가 운영하는 K-Beauty 유통 플랫폼이고, 이 사이트는 그중 **K-Beauty Growth Program**의 파트너 브랜드를 모집한다.
>
> **읽는 사람:** 한국 중소·인디 뷰티 브랜드의 대표와 해외영업 담당자. 30–50대, 데스크톱으로 본다. 미국 진출을 원하지만 세 가지가 두렵다 — 선금을 보낸 뒤 연락이 끊기는 것, 병행수입으로 가격과 브랜드 이미지가 무너지는 것, 대량 생산 없이는 진출이 불가능하다는 인식. 이 사람은 마케팅 문구를 믿지 않고 숫자와 정책을 믿는다.
>
> **이 페이지가 증명해야 할 단 하나:** Letusto는 리서치·컨설팅 회사가 아니라, 미국에서 실제로 매장과 온라인 채널을 직접 운영하는 사업자다.
>
> **톤:** 절제된 확신. 감탄사와 마케팅 형용사를 쓰지 않는다. "혁신적인", "최고의", "여러분의 성공" 같은 표현 금지. 숫자와 고유 정책명으로 말한다.
>
> **비주얼 방향:** 스위스 편집 디자인의 절제된 그리드 위에 초대형 타이포그래피. 장식 요소 없이 타입 크기 대비와 여백만으로 위계를 만든다. 스톡 사진을 쓰지 않고 모든 비주얼을 계산된 SVG 도형(지도, 바 차트, 넘버링 레일)으로 만든다. 구조는 다크 히어로 → 라이트 본문 → 다크 CTA.
>
> **컬러:** 다크 배경 `#0E0E10`(웜 니어블랙), 다크 위 텍스트 `#F4F0E8`(아이보리), 라이트 배경 `#FAF9F6`(웜 오프화이트), 본문 `#16171A`, 보조 텍스트 `#6B6E76`, 구분선 `#E1DED6`. 액센트는 `#1450E0` 딥 블루 **하나뿐이고, 한 화면에 최대 2회만** 등장한다 — 헤드라인 속 단어 하나와 주 CTA 버튼. 그라디언트와 컬러 블록에는 절대 쓰지 않는다.
>
> **타이포:** Pretendard Variable. 히어로 헤드라인은 한글 96px / 800 웨이트. 한글 대형 조판이므로 `letter-spacing: -0.035em`, `line-height: 1.16`, `word-break: keep-all`을 반드시 적용한다. 헤드라인 위에는 11px / `letter-spacing: 0.18em` 대문자 라틴 마이크로 라벨을 놓는다. 액센트로 영문 단어 하나에만 Instrument Serif Italic을 쓸 수 있다.
>
> **홈에 들어갈 내용과 실제 문구 (창작하지 말고 이 값을 사용):**
> - 히어로 라벨: `K-BEAUTY GROWTH PROGRAM — OPERATED BY LETUSTO INC.`
> - 히어로 서브: "낯설고 두려운 미국 진출이 아니라, 검증된 파트너와 함께 시작하는 성장입니다."
> - 지표 4개: 설립 2004 / 누적 판매 US$250M+ / 운영 브랜드 10+ / 거점 3개국(미·한·중)
> - 시장 근거: 2025 미국 K-Beauty 시장 $27.6B, CAGR 9.5%(2026–2033), 글로벌 점유율 23.4%, 2025 대미 수출 $2.2B
> - 문제 5개(선금 후 연락 두절 / 대형 유통의 높은 진입장벽 / 소량으로 시장 반응 확인할 창구 부재 / 인증·라벨링·통관 정보 부족 / 병행수입으로 브랜드 통제력 상실)를 좌측 다크, 해법 4개(온·오프라인 동시 런칭 / Store-in-a-Store 진열 / 상권 보호 정책 / First Order Protection)를 우측 라이트에 좌우로 붙여 대조시킨다. 이 대조가 페이지에서 가장 강한 시각적 순간이어야 한다.
> - 카테고리 비중을 CSS 바 차트로: K-스킨케어 75%, K-헤어&스칼프 10%, K-뷰티 소품/툴 5%, K-데일리 케어 5%, K-웰니스/기능성 패치 5%
> - 절차 9단계를 카드 그리드가 아니라 수평 넘버링 레일로. 1단계 테스트 파트너십(문의 → 자격 검토 → 샘플 제출 → 분석 리포트 공유 → 테스트 계약), 2단계 본 파트너십(테스트 런칭 → 데이터 검증 → 결과 평가 → 본계약 및 발주 확대)
> - 최종 CTA: 다크 섹션. "파트너 자격 자가진단 시작하기"
> - 상단 플로팅 pill 내비게이션: 좌측 `K Select Network` 타입 워드마크, 메뉴(프로그램 / 왜 Letusto인가 / 파트너십 혜택 / 진행 절차 / 참여 자격 / 자료실 / 문의), 우측 KR·EN 토글과 주 CTA
>
> **하지 말 것:** Inter나 Roboto 폰트, 보라·바이올렛 그라디언트, 글래스모피즘, 이모지 아이콘, 동일한 아이콘 카드 3개를 중앙 정렬한 그리드, 흐릿한 "Trusted by" 로고 행, 전체 폭 히어로 배경 사진, 웃는 모델이나 악수 스톡 사진, 라운드 24px 이상의 물렁한 카드, 그라디언트로 채운 텍스트.
>
> **요청:** 위 조건을 지키면서 **히어로와 문제→해법 대조 섹션의 구성이 서로 다른 3가지 변형**을 보여줘라. 세 안 모두 컬러와 타이포는 동일하게 유지하고, 레이아웃 전략과 정보 배치만 다르게 한다.

---

## PART 4. 후속 프롬프트 (홈 확정 후, 하나씩)

**자가진단 페이지 — 가장 중요한 화면**
> `/apply/check` 자가진단 페이지를 만들어라. 참여 조건 6개를 세로 리스트로 놓고, 각 항목을 전체 영역이 클릭되는 큰 체크 카드로 만든다. 선택 시 좌측에 4px `#1450E0` 바가 나타난다. 하단에 "4 / 6 충족" 형태의 고정 진행 표시를 두고, 6개 모두 충족될 때만 하단 CTA가 `#1450E0`로 활성화된다. 미충족 항목이 있는 경우는 빨강이 아니라 `#8A5A2B` 웜 브라운으로 표시하고, 거절이 아니라 "다음 단계 준비 안내"로 읽히게 한다. 이 페이지는 집중 모드다 — 상단 내비를 워드마크와 종료 버튼만 남기고 축소한다.
> 6개 조건: ① 리오더에도 안정적으로 재생산·공급 가능한 생산 캐파 ② 미국 상표권 등록 또는 진행 의지, 성분·라벨링 인증 대응 ③ 초도 테스트 물량 300개(오프라인 240 + 온라인 60) 공급 가능 ④ 타 총판·이커머스와 중복 없이 공급 우선권 제공 가능 ⑤ 목표 달성 시 마케팅 비용 일부 분담 의지 ⑥ 제품 이미지·사용법·루틴 등 판매용 콘텐츠 제공 가능

**신청폼**
> `/apply/form` 정식 신청폼. 1열 세로 배치. 브랜드 정보 / 상품 정보 / 생산 역량 / 담당자 4개 섹션으로 나누고 각 섹션에 로마숫자 넘버링과 hairline 구분을 준다. 라벨은 필드 위 11px 대문자 마이크로 라벨, focus 시 2px `#1450E0` ring. 상단에 진행률 바를 고정한다. 자가진단을 통과한 사람만 오는 화면이므로, 상단에 "자가진단 6개 항목 충족 확인됨" 상태 배지를 둔다.

**왜 Letusto인가**
> `/why-letusto`. 세 개 블록으로 "우리는 컨설팅 회사가 아니다"를 증명한다 — ① 회사 이력과 지표(2004 설립, US$250M+, 10+ 브랜드, 3개국) ② 미국 매장 네트워크를 인라인 SVG 지도로. 현재 거점은 `#1450E0` 점, 확장 예정 지역은 hairline 링. 전국을 넓게 덮는 방식이 아니라 핵심 지역에 밀도를 먼저 만들고 확장한다는 전략을 지도로 읽히게 한다 ③ 파트너십 확정 즉시 제공하는 4종 자료(시장 조사 / 경쟁사 조사 / 인증 현황 / 가격 분석)와 판매 개시 후 주간·월간 실판매 데이터 검증. "감이 아니라 데이터로 함께 성장합니다"를 이 블록의 결론 문장으로 쓴다. 지표 블록만 다크 밴드로 처리한다.

**미팅 예약**
> `/apply/meeting`. Microsoft Bookings iframe을 임베드할 페이지다. iframe이 들어갈 영역은 최대폭 720px 중앙 배치로 잡고, 그 위에 미팅 전 준비물(브랜드 소개 자료, 상품 리스트, MOQ 및 리드타임)을 3개 항목으로 안내한다. iframe 영역은 hairline 보더 + 12px 라운드로 감싸 사이트 디자인에 붙게 한다.

---

## PART 5. 막혔을 때 쓰는 프롬프트

| 상황 | 프롬프트 |
|---|---|
| 결과가 평범하다 | "이건 버리고, 훨씬 절제되고 우아한 버전으로 다시 해라. 요소 수를 30% 줄이고 여백을 늘려라." |
| 액센트 색이 남발됐다 | "이 화면에서 `#1450E0`이 몇 번 쓰였는지 세고, 헤드라인 단어 하나와 주 CTA 두 곳만 남기고 전부 `#16171A` 또는 `#6B6E76`으로 바꿔라." |
| 한글이 안 예쁘다 | "한글 대형 텍스트의 자간을 `-0.035em`, 행간을 `1.16`으로, `word-break: keep-all`을 적용하고 헤드라인 줄바꿈을 의미 단위로 수동 지정해라." |
| AI 티가 난다 | "동일한 카드가 반복되는 그리드를 전부 비대칭 배치로 바꿔라. 좌측 5컬럼 텍스트 + 우측 6컬럼 데이터 + 1컬럼 공백 형태로." |
| 신뢰감이 약하다 | "숫자를 더 크게, 수식어를 더 작게. 형용사 문장을 지우고 데이터와 고유 정책명으로 대체해라." |
| 방향이 갈린다 | "이 섹션만 3가지 변형으로 보여줘라. 컬러와 타이포는 고정하고 레이아웃만 다르게." |

---

## PART 6. Claude Code로 넘길 때

Handoff 직후 Claude Code에 던질 첫 지시:

> Claude Design에서 넘어온 디자인을 Next.js(App Router) + TypeScript + Tailwind CSS로 구현한다. DESIGN.md의 컬러·타이포·스페이싱을 CSS 변수로 선언한 뒤 Tailwind theme에 매핑해서, 하드코딩된 색상값이 코드에 하나도 남지 않게 해라. Pretendard Variable과 Instrument Serif를 self-host한다. 한글 조판 규칙(자간 -0.035em, 행간 1.16, word-break: keep-all)은 유틸리티 클래스로 만들어 대형 텍스트에 일괄 적용한다. `prefers-reduced-motion`을 반드시 처리한다. 자가진단은 클라이언트 상태로 6개 조건을 관리하고 전부 충족 시에만 신청폼 라우트로 진입 가능하게 한다. Git 저장소를 초기화하고 Vercel 배포 설정까지 잡아라.

---

## 미해결 항목

1. **명칭 통일** — 소개서 PDF에는 유통 플랫폼이 "K-Beauty Retail Network"로 적혀 있다. 사이트가 K Select Network라면 PDF도 수정해야 한다. 이미 발송한 자료가 있는지 확인이 필요하다.
2. **웹 주소 확정** — K Select Network의 실제 도메인. Vercel 연결과 영문 표기(kselectnetwork.com?)에 필요하다.
3. **매장 실사진 3종** — Beauty Supply 매장 외관, Store-in-a-Store 진열 실물, 제품 진열대. 신뢰성의 절반이 여기서 나온다. 확보 전까지는 SVG 비주얼로 대체.
4. **한국 지사 연락처** — PDF에 "별도 안내 예정"으로 남아 있다. 한국 브랜드가 대상이므로 한국 연락처 부재는 신뢰에 불리하다.
5. **Microsoft Bookings 페이지** — 임베드할 실제 예약 페이지 URL.
