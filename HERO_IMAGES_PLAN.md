# 히어로 이미지 2슬롯 — 확보 · 애니메이션 · Claude Code 지시

K Select Network 메인 화면

---

## 0. 먼저 짚어야 할 것: 이 두 장은 스톡을 쓰면 안 됩니다

이 사이트 전체의 주장은 **"Letusto는 리서치·컨설팅 회사가 아니라 미국에서 실제로 매장을 운영하는 사업자다"** 하나입니다. 그리고 히어로의 이 두 이미지가 그 주장을 시각적으로 증명하는 유일한 자리입니다.

여기에 스톡 사진을 넣으면 아래가 일어납니다.

- 뷰티 유통 담당자는 미국 Beauty Supply 매장의 실제 모습을 압니다. 스톡의 깔끔한 화장품 부티크 사진은 즉시 걸립니다.
- 한국 브랜드사가 이미지를 역검색하면 스톡임이 드러납니다. 그 순간 "$250M+"와 "10개 매장"까지 함께 의심받습니다.
- AI 생성 매장 이미지는 최악입니다. 그럴듯하지만 업계 사람은 알아보고, 사실상 허위 표시입니다.

**결론: 이 두 슬롯은 실사진으로만 채우고, 실사진이 준비될 때까지는 사진이 아닌 것으로 버팁니다.** 아래 4번의 대체안이 그것입니다.

---

## 1. 두 슬롯의 정의

| 슬롯 | 위치 | 비율 | 렌더 크기 | 담아야 할 것 |
|---|---|---|---|---|
| **A** | 우측 상단 (대형) | **4:3** 가로 | 약 506 × 381px | Beauty Supply 매장 **외관**. 간판과 입구가 보이고, 미국 로컬 상권임이 읽혀야 한다 |
| **B** | 우측 하단 좌측 (소형) | **1:1** 정사각 | 약 246 × 246px | **Store-in-a-Store** 진열. K-Beauty 섹션의 선반과 제품이 보여야 한다 |

슬롯 A가 "우리는 실재한다", 슬롯 B가 "우리는 당신 제품을 이렇게 놓는다"를 담당합니다. 역할이 다르므로 둘 다 필요합니다.

**서로 다른 매장의 사진을 쓰지 마세요.** 같은 매장의 외관과 내부 진열이면 두 장이 하나의 증거로 연결됩니다. 다른 매장이면 그냥 사진 두 장입니다.

---

## 2. 1순위 — 직접 촬영 (권장)

미국에 실제 매장 네트워크가 있으니 이게 가장 빠르고 가장 강력합니다. 스마트폰으로 충분합니다. 현지 담당자나 매장주에게 아래 샷 리스트를 그대로 전달하세요.

### 슬롯 A — 매장 외관 촬영 지시

- **각도**: 정면에서 좌우로 30~45도 비껴선 대각. 완전 정면은 평면적이고, 유리에 촬영자가 반사됩니다.
- **프레임**: 간판 전체 + 입구 + 좌우 인접 점포가 살짝. 매장만 잘라내지 말 것 — 상권 안에 있다는 게 보여야 합니다.
- **시간**: 흐린 날 낮, 또는 해 지기 1~2시간 전. 정오 직사광은 간판에 강한 그림자와 반사를 만듭니다.
- **높이**: 눈높이. 아래에서 올려찍으면 왜곡됩니다.
- **비율**: 가로. 4:3보다 넓게(16:9 정도로) 찍어두면 크롭 여유가 생깁니다.
- **디테일**: 주차된 차량, 보도, 영업 중 표시가 프레임에 들어오면 실재감이 올라갑니다. 사람 얼굴은 프레임 밖으로.
- **수량**: 매장 3곳 이상 × 각 5장. 선택지가 있어야 합니다.

### 슬롯 B — Store-in-a-Store 진열 촬영 지시

- **각도**: 선반 정면, 살짝 사선(15도 정도)으로 깊이감. 선반 수평선이 기울지 않게.
- **프레임**: K-Beauty 섹션 헤더나 사이너지가 함께 보이면 최상. 정사각으로 크롭될 것을 감안해 **가운데를 꽉 채워서** 찍습니다.
- **조명**: 매장 조명 그대로. **플래시 금지** — 제품 패키지에 반사돼 하이라이트가 터집니다.
- **해상도**: 제품 라벨의 브랜드명이 읽힐 정도로 가까이. 너무 멀면 그냥 잡화 선반으로 보입니다.
- **정리**: 촬영 전 선반 정면 정리, 빈 자리 채우기, 가격표 정렬. 흐트러진 선반은 관리가 안 되는 매장으로 읽힙니다.
- **수량**: 3~5개 앵글.

### 파일 스펙

- JPEG, 최고 화질. HEIC로 받으면 변환 과정에서 손실이 생기니 JPEG로 지정.
- 장변 **2400px 이상**. 원본은 별도 보관.
- 촬영자·촬영일·매장명을 파일명에 남길 것: `store-exterior_atlanta-01_2026-07.jpg`

---

## 3. 2순위 — 기존 자산 발굴

촬영 전에 이미 있는 것부터 찾습니다. 의외로 나옵니다.

- 매장 오픈·리뉴얼 당시 사진
- 진열 검수·발주 확인용으로 찍어둔 사진 (품질은 낮아도 크롭하면 쓸 만한 경우가 많음)
- 파트너 브랜드에 진열 인증으로 보낸 사진
- 매장의 **Google Business Profile** 등록 사진 — 소유 계정이면 원본 다운로드 가능
- 매장 또는 Letusto의 SNS 게시물 원본
- 매장주에게 요청: "간판이 보이는 외관 사진과 K-Beauty 선반 사진 몇 장 보내주세요"

**주의**: 남의 매장을 무단으로 찍은 사진, 경쟁사 매장 사진, 타 브랜드 상표가 크게 노출된 사진, 인물 얼굴이 식별되는 사진은 쓰지 않습니다. 미국 상업 웹사이트라 초상권·상표 문제가 실제로 발생합니다.

---

## 4. 사진이 없을 때 — 사진 대신 넣을 것 (개발 병행용)

지금 개발을 멈추지 않고 진행하려면, **스톡을 임시로 끼우는 대신 사진이 아닌 비주얼로 슬롯을 채웁니다.** 나중에 사진으로 교체할 때 레이아웃이 흔들리지 않도록 같은 비율을 유지합니다.

| 슬롯 | 대체 비주얼 |
|---|---|
| **A** (4:3) | **미국 매장 네트워크 SVG 지도.** 현재 거점은 채운 점, 확장 예정 지역은 얇은 링. 하단에 `10 STORES · 3 COUNTRIES` 마이크로 라벨. 사진보다 정보량이 많아서 오히려 이 자리에 어울립니다. |
| **B** (1:1) | **선반 다이어그램 SVG.** 매대 3~4단을 선으로 그리고 한 구획을 액센트로 강조해 "이 자리가 당신 브랜드의 섹션"을 표현. 하단에 `STORE-IN-A-STORE` 라벨. |

현재 들어가 있는 대각선 스트라이프 플레이스홀더는 "이미지 없음"으로 읽힙니다. 위 두 SVG는 "의도된 도해"로 읽힙니다. 차이가 큽니다.

**스톡을 정말 임시로 써야 한다면** 아래 조건을 전부 지키고, 커밋 메시지와 코드 주석에 `TODO: replace with real store photo before launch`를 남깁니다.

- 라이선스: Unsplash / Pexels(무료, 상업 이용 가능) 또는 Adobe Stock(유료, 라이선스 안전)
- 사람 얼굴 없음, 읽히는 경쟁 브랜드 상표 없음
- 한국 화장품 매장 사진 금지 — 미국 매장이라고 하면서 한국 매장을 쓰는 건 허위입니다
- 프로덕션 배포 전 반드시 교체. 스톡이 붙은 상태로 도메인을 공개하지 않습니다

---

## 5. 애니메이션 스펙

### 확정안

두 이미지에는 **프레임 진입 + 내부 이미지 세틀** 2층 구조를 씁니다.

```
프레임 (외곽, overflow: hidden)
  opacity   0 → 1
  translateY 20px → 0
  duration  600ms
  easing    cubic-bezier(0.16, 1, 0.3, 1)

이미지 (프레임 내부)
  scale     1.06 → 1.0
  duration  900ms   ← 프레임보다 길게
  easing    동일
```

프레임이 먼저 자리를 잡고, 그 안에서 이미지가 조금 더 오래 천천히 정착합니다. 프레임 자체는 움직이지 않으므로 레이아웃이 흔들리지 않고, 사진이 "놓이는" 감각이 생깁니다. 이게 편집 디자인에서 사진을 고급스럽게 보이게 하는 가장 저렴하고 확실한 방법입니다.

- **스태거**: 슬롯 A 먼저, 슬롯 B는 **120ms 지연**. 지표 카드($250M+)는 240ms 지연.
- **트리거**: 1회만. 재진입 시 다시 재생하지 않습니다.
- **hover**: `scale 1.0 → 1.015`, 600ms. 아주 미세하게. 커서를 올렸다는 반응만 있으면 됩니다. 확대감이 느껴지면 과합니다.
- **로딩**: `placeholder="blur"` LQIP. 로드 전에는 현재의 스트라이프 플레이스홀더가 아니라 흐린 저해상도 원본이 보이게 합니다.

### 쓰지 않는 것

패럴랙스 스크롤, clip-path 와이프(위 scale과 중복되어 과함), 무한 부유 애니메이션, Ken Burns 슬로우 줌, 3D 틸트, 마우스 추적 시차.

### DESIGN.md 예외 처리 — 중요

`DESIGN.md`에는 **"scale 기반 등장 금지, transform은 translateY만"** 이라고 적어뒀습니다. 위 스펙은 그 규칙의 예외입니다. 근거는 이렇습니다.

금지한 것은 **요소 전체가 커지면서 등장하는(pop-in) 움직임**입니다. 위 스펙의 scale은 **고정된 프레임 안에서 이미지 콘텐츠만** 움직이는 것이고, 프레임의 바운딩 박스는 변하지 않습니다. 시각적으로 전혀 다른 효과이고, 레이아웃 시프트도 없습니다.

이 예외를 `DESIGN.md`에 명시적으로 추가해야 합니다. 그러지 않으면 다음 작업에서 규칙 위반으로 되돌려집니다. 아래 프롬프트에 그 지시를 포함했습니다.

### 접근성 — 필수

```css
@media (prefers-reduced-motion: reduce) {
  /* 모든 등장·hover 애니메이션 제거, 즉시 최종 상태 */
}
```

옵션이 아닙니다.

### LCP 주의 — 놓치기 쉬운 부분

슬롯 A는 히어로 우측 최상단에 있어 **LCP(Largest Contentful Paint) 요소가 될 가능성이 높습니다.**

`whileInView`로 opacity 0에서 시작하게 만들면 LCP 측정이 지연되고 Lighthouse 점수가 떨어집니다. 이미 첫 화면에 보이는 요소를 스크롤 트리거에 걸 이유도 없습니다.

- 슬롯 A와 B는 **스크롤 트리거를 쓰지 않고 마운트 즉시 재생**합니다.
- 슬롯 A에 `priority` (Next.js `<Image priority />`)를 부여합니다.
- `width`/`height` 또는 `aspect-ratio`를 반드시 명시해 CLS를 0으로 유지합니다.
- 진입 애니메이션의 opacity 시작값을 0이 아니라 0.001로 두거나, 이미지 자체는 즉시 불투명하게 두고 scale 세틀만 적용하는 방식을 검토합니다.

---

## 6. Claude Code 프롬프트 (그대로 복사)

> 아래 `===` 사이 전체를 붙여넣으세요.

===

메인 히어로 우측의 이미지 슬롯 2개를 구현한다. 저장소의 기존 디자인 토큰만 사용하고, 새 색상 값을 하드코딩하지 않는다.

## 슬롯 정의

| 슬롯 | 컴포넌트 위치 | 비율 | 내용 |
|---|---|---|---|
| A | 히어로 우측 상단 (대형) | 4:3 | Beauty Supply 매장 외관 |
| B | 히어로 우측 하단 좌측 (소형) | 1:1 | Store-in-a-Store 진열 |

두 슬롯을 `<HeroFrame>` 이라는 **단일 재사용 컴포넌트**로 만든다. props: `src`, `alt`, `ratio`("4:3" | "1:1"), `caption`, `delay`, `priority`, `fallback`.

## 이미지 자산 처리 — 교체 가능한 구조로

실사진이 아직 준비되지 않았다. 지금은 사진 없이 동작하고, 사진이 들어오면 **파일만 넣고 데이터 한 줄만 바꾸면** 교체되는 구조로 만든다.

1. `lib/hero-media.ts` 에 슬롯 데이터를 분리한다.

```ts
export const heroMedia = {
  storeExterior: {
    src: null,                 // '/images/hero/store-exterior.jpg' 로 교체
    alt: '미국 현지 Beauty Supply 매장 외관',
    caption: 'Beauty Supply 매장 외관',
    ratio: '4:3' as const,
  },
  storeInStore: {
    src: null,                 // '/images/hero/store-in-store.jpg' 로 교체
    alt: 'K-Beauty 전용 Store-in-a-Store 진열대',
    caption: 'Store-in-a-Store 진열',
    ratio: '1:1' as const,
  },
}
```

2. `src`가 `null`이면 **스톡 사진이나 회색 플레이스홀더를 넣지 말고**, 아래 SVG 폴백을 렌더한다.

   - 슬롯 A 폴백: 미국 매장 네트워크를 나타내는 인라인 SVG. 현재 거점은 채운 원, 확장 예정 지역은 1px 링. 우하단에 `10 STORES · 3 COUNTRIES` 마이크로 라벨. 지도는 정밀할 필요 없고 추상화된 형태로 충분하다.
   - 슬롯 B 폴백: 매대 선반 3~4단을 선으로 그린 다이어그램. 한 구획만 액센트 색으로 채워 "이 자리가 당신의 섹션"을 표현. 하단에 `STORE-IN-A-STORE` 마이크로 라벨.
   - 두 폴백 모두 현재의 대각선 스트라이프 플레이스홀더를 대체한다. 스트라이프는 "이미지 없음"으로 읽혀서 신뢰를 깎는다.

3. `src`가 채워지면 Next.js `<Image>`로 렌더한다. `fill` + `object-cover`, `sizes` 명시, 슬롯 A는 `priority`, `placeholder="blur"`.

4. `public/images/hero/` 디렉토리를 만들고 `README.md`에 필요한 파일명·비율·최소 해상도(장변 2400px 이상)를 적어둔다.

## 애니메이션

프레임과 내부 이미지를 2층으로 분리해 움직인다.

```
프레임: opacity 0 → 1, translateY 20px → 0, 600ms, cubic-bezier(0.16, 1, 0.3, 1)
이미지: scale 1.06 → 1.0, 900ms, 동일 이징   ← 프레임보다 길게
```

프레임은 `overflow: hidden`이고 바운딩 박스는 변하지 않는다. 이미지 콘텐츠만 안에서 정착한다.

- 스태거: 슬롯 A `delay 0`, 슬롯 B `delay 120ms`, 우하단 지표 카드 `delay 240ms`
- hover: `scale 1.0 → 1.015`, 600ms. 아주 미세하게만.
- 1회만 재생. 재진입 시 반복하지 않는다.

**쓰지 않을 것:** 패럴랙스, clip-path 와이프, 무한 부유 애니메이션, Ken Burns 줌, 3D 틸트, 마우스 추적.

## LCP 및 성능 — 반드시 지킬 것

- 히어로 이미지는 첫 화면에 이미 보이므로 **`whileInView` 스크롤 트리거를 쓰지 않는다.** 마운트 즉시 재생한다. 스크롤 트리거를 걸면 LCP가 지연된다.
- 슬롯 A에 `priority`를 부여한다.
- `aspect-ratio`를 CSS로 명시해 CLS를 0으로 유지한다. 이미지 로드 전후로 레이아웃이 움직이면 안 된다.
- 진입 애니메이션이 LCP 측정을 방해하지 않도록, 이미지 요소 자체의 opacity는 즉시 1로 두고 프레임 래퍼에만 페이드를 적용하는 방식을 검토한다. 구현 후 Lighthouse로 LCP를 측정해 보고해라.

## 접근성

- `prefers-reduced-motion: reduce`에서 모든 등장·hover 애니메이션을 제거하고 즉시 최종 상태로 렌더한다. 필수다.
- `alt` 텍스트는 위 데이터의 값을 사용한다. "이미지"나 빈 문자열을 쓰지 않는다.
- SVG 폴백에는 `role="img"`와 `aria-label`을 부여한다.
- 캡션은 이미지 위 오버레이가 아니라 프레임 하단 안쪽에 마이크로 라벨 스타일로 배치하고, 이미지가 어두울 경우에도 대비가 유지되도록 한다.

## DESIGN.md 갱신

`DESIGN.md`의 모션 섹션에 다음 예외를 추가해라.

> **예외 — 이미지 프레임 내부 세틀:** 고정된 `overflow: hidden` 프레임 안에서 이미지 콘텐츠에만 적용하는 `scale 1.06 → 1.0` 세틀은 허용한다. 프레임의 바운딩 박스가 변하지 않으므로 금지 대상인 요소 전체의 pop-in scale과 구분된다. 프레임 래퍼에는 여전히 opacity와 translateY만 사용한다.

## 완료 조건

- [ ] `HeroFrame` 단일 컴포넌트로 두 슬롯이 렌더되고, 비율이 각각 4:3 / 1:1로 고정된다
- [ ] `heroMedia.src`를 `null`에서 실제 경로로 바꾸는 것만으로 사진 교체가 완료된다
- [ ] `src: null` 상태에서 대각선 스트라이프가 아니라 SVG 폴백이 보인다
- [ ] `prefers-reduced-motion: reduce`에서 애니메이션이 전부 사라진다
- [ ] CLS 0, 슬롯 A가 LCP 요소이며 `priority`가 적용되어 있다 (Lighthouse 수치 보고)
- [ ] 모바일 375px에서 우측 이미지 그룹이 텍스트 아래로 떨어지고 비율이 유지된다
- [ ] 새로 추가한 코드에 하드코딩된 HEX 색상값이 없다
- [ ] `npm run build`와 타입 체크 통과

변경한 파일 목록과, 확인하지 못한 체크리스트 항목을 솔직하게 알려줘라.

===

---

## 7. 별건 — 내비게이션이 다시 늘었습니다

이번 캡처의 상단 메뉴가 `PROGRAM / LETUSTO / POLICY / PROCESS / ELIGIBILITY / FAQ / EXPORT VOUCHER` **7개**입니다. `EXPORT VOUCHER`가 추가됐습니다.

앞서 드린 `CC_PROMPT_nav-and-benefits.md`가 아직 적용되지 않은 상태로 보입니다. 그 프롬프트를 적용하면 최상단이 `KBG PROGRAM ▾ / LETUSTO / FAQ`로 정리되는데, `EXPORT VOUCHER`를 어디에 둘지 결정이 필요합니다.

- 정부 수출바우처 관련 안내라면 → `KBG PROGRAM` 드롭다운 안 또는 `자료실`로
- 참여 유인이 되는 핵심 혜택이라면 → 최상단에 남기고 총 4개

지원사업 연계는 중소기업에게 강한 유인이라 최상단 유지도 근거가 있습니다. 어느 쪽인지 알려주시면 내비 프롬프트를 수정해 드리겠습니다.
