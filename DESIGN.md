# DESIGN.md — K Select Network

> **K-Beauty Growth Program** 파트너십 사이트 디자인 시스템. 운영: Letusto Inc.
> Claude Design 온보딩 시 이 문서를 첨부한다. Claude Code 개발 단계에서도 동일 문서를 기준으로 삼는다.

---

## 0. 네이밍 계층 (혼동 금지)

세 개의 이름이 각각 다른 층에 있다. 화면에서 역할을 섞지 않는다.

| 이름 | 층 | 화면에서의 위치 |
|---|---|---|
| **K Select Network** | 사이트/플랫폼 브랜드 (웹 주소) | 좌상단 워드마크, 브라우저 타이틀, 푸터 |
| **K-Beauty Growth Program** | 홍보 대상 — 이 사이트가 파는 것 | 히어로 헤드라인 위 마이크로 라벨, 모든 페이지의 주제 |
| **Letusto Inc.** | 운영 주체 — 신뢰의 근거 | 히어로 라벨의 "operated by", 회사 소개 페이지, 푸터 법적 표기 |

**히어로 상단 라벨 규격:**
`K SELECT NETWORK` (워드마크) / 헤드라인 위 라벨은 `K-BEAUTY GROWTH PROGRAM — OPERATED BY LETUSTO INC.` 한 줄, `label` 스타일.

워드마크는 로고 이미지가 없으므로 **타입 워드마크**로 만든다: `K Select` 700 + `Network` 400, 자간 `-0.02em`, 단색. 아이콘·심볼을 창작해 붙이지 않는다.

---

## 1. 프로젝트 정의

| 항목 | 내용 |
|---|---|
| 사이트 | K Select Network — K-Beauty Growth Program 파트너 모집 (운영: Letusto Inc.) |
| 목적 | 프로그램 소개 → 참여 자격 자가진단 → 정식 신청 접수 → 미팅 예약 |
| 대상 | 한국 중소·인디 뷰티 브랜드의 대표 및 해외영업 담당자 (30–50대, 데스크톱 우선, 의사결정권자) |
| 이 사람이 두려워하는 것 | 선금 보내고 연락 두절, 병행수입으로 가격 붕괴, 대량 생산 없이는 진출 불가라는 인식 |
| 사이트가 증명해야 할 단 하나 | "Letusto는 리서치·컨설팅 회사가 아니라, 미국에서 실제 매장을 운영하는 사업자다" |
| 톤 | 절제된 확신. 과장·감탄사·마케팅 형용사 없이 사실과 숫자로 신뢰를 만든다. |
| 언어 | 한국어 우선 + 영문 토글 |

**금지된 톤:** "혁신적인", "최고의", "여러분의 성공을 위해" 류의 공허한 수식. 숫자와 정책 이름으로 말한다.

---

## 2. 비주얼 컨셉 — "Editorial Trust"

스위스 편집 디자인의 절제된 그리드 + 초대형 타이포그래피를 기반으로 한다.
장식적 요소를 쓰지 않고, **타이포그래피의 크기 대비**와 **여백**만으로 위계를 만든다.
비주얼은 사진에 의존하지 않고 계산된 도형(SVG 차트, 지도, 넘버링 레일)으로 만든다.

**구조 원칙: 다크 히어로 → 라이트 본문**

- 히어로 섹션과 최종 CTA 섹션만 딥 잉크 배경.
- 프로그램 설명·절차·자격·신청폼 등 읽고 입력하는 모든 화면은 웜 오프화이트.
- 이 대비 자체가 "선언 → 근거" 구조를 시각적으로 만든다.

---

## 3. 컬러

정확히 이 값만 사용한다. 새로운 색을 추가하지 않는다.

| 토큰 | HEX | 용도 |
|---|---|---|
| `ink` | `#0E0E10` | 다크 섹션 배경. 웜 니어블랙 — 파란기 도는 검정 금지 |
| `ink-raised` | `#17171A` | 다크 섹션 내 패널 |
| `ivory` | `#F4F0E8` | 다크 섹션의 헤드라인·본문 텍스트 |
| `paper` | `#FAF9F6` | 라이트 섹션 배경. 순백(#FFF) 및 쿨그레이 금지 |
| `paper-raised` | `#F2F0EA` | 라이트 섹션 내 패널·카드 |
| `graphite` | `#16171A` | 라이트 섹션 본문 텍스트 |
| `slate` | `#6B6E76` | 보조 텍스트, 캡션, 라벨 |
| `hairline` | `#E1DED6` | 1px 구분선 (라이트) |
| `hairline-dark` | `#2A2A2E` | 1px 구분선 (다크) |
| `accent` | `#1450E0` | **유일한 액센트.** 깊은 인스티튜셔널 블루 |
| `accent-ink` | `#0B2E8A` | 액센트 hover·press |
| `warn` | `#8A5A2B` | 자가진단 미충족 표시. 빨강 대신 웜 브라운 — 거절이 아니라 안내 |

**액센트 사용 규칙 — 이것을 지키는 것이 이 디자인의 핵심이다.**
액센트는 한 화면(1 viewport)에 **최대 2회**만 등장한다. 허용되는 곳은 다음뿐이다:

1. 헤드라인 안의 단어 **하나**
2. 주 CTA 버튼 배경
3. 절차 섹션의 스텝 번호
4. 폼 입력 필드의 focus ring
5. 데이터 바 차트의 활성 바

배경 그라디언트, 큰 컬러 블록, 아이콘 채색에는 절대 쓰지 않는다.

**왜 블루인가:** 이미 잠재 파트너에게 발송 중인 소개서 PDF가 블루 계열이다. 자료와 사이트의 색이 일치하는 것 자체가 B2B에서 신뢰 신호다. 다만 PDF의 밝은 블루보다 채도를 낮추고 어둡게 조정해 SaaS 사이트 느낌을 없앤다.

---

## 4. 타이포그래피

### 폰트

| 역할 | 폰트 | 비고 |
|---|---|---|
| 한글 + 라틴 본문/헤드라인 | **Pretendard Variable** (100–900) | 기본. CDN 로드 |
| 라틴 마이크로 라벨 | Pretendard 500, uppercase, 트래킹 확장 | 별도 폰트 불필요 |
| 액센트 단어 (라틴 전용) | **Instrument Serif Italic** | 히어로에서 영문 단어 1개에만. 레퍼런스의 이탤릭 스크립트 역할 |

Inter, Roboto, Noto Sans KR, 맑은 고딕 사용 금지.

### 스케일

| 이름 | 데스크톱 | 모바일 | 굵기 | 용도 |
|---|---|---|---|---|
| `display` | 96px | 44px | 800 | 히어로 헤드라인 |
| `h1` | 60px | 34px | 800 | 페이지 타이틀 |
| `h2` | 40px | 28px | 700 | 섹션 타이틀 |
| `h3` | 24px | 20px | 700 | 카드 타이틀 |
| `stat` | 72px | 44px | 800 | 지표 숫자 (tabular-nums) |
| `body-lg` | 20px | 17px | 400 | 리드 문단 |
| `body` | 17px | 16px | 400 | 본문 |
| `caption` | 14px | 13px | 400 | 캡션, 각주 |
| `label` | 11px | 11px | 500 | 마이크로 라벨. `letter-spacing: 0.18em`, uppercase |

### 한글 대형 조판 규칙 (반드시 준수)

영문 기준으로 조판하면 한글은 무너진다. 다음을 강제한다.

```css
/* display / h1 / h2 등 24px 이상 */
letter-spacing: -0.035em;   /* 한글은 영문보다 자간을 더 좁혀야 덩어리로 읽힌다 */
line-height: 1.16;          /* 영문의 0.95는 한글에서 받침이 붙어 읽히지 않는다 */
word-break: keep-all;       /* 단어 중간 줄바꿈 금지 */
overflow-wrap: break-word;

/* 본문 */
letter-spacing: -0.01em;
line-height: 1.7;           /* 한글 본문은 영문보다 행간이 넉넉해야 한다 */
```

- 한글 텍스트에 `font-synthesis` 가짜 볼드 금지. 실제 700/800 웨이트만 사용.
- 한글 최소 크기 15px. 그 아래는 라틴 라벨에만 허용.
- 헤드라인 줄바꿈은 자동에 맡기지 않고 의미 단위로 `<br>` 수동 삽입.
- 마이크로 라벨은 라틴/숫자만. 한글에 `letter-spacing: 0.18em`을 주면 글자가 흩어진다.

---

## 5. 레이아웃

- 12 컬럼 그리드, 컨테이너 최대폭 `1280px`, 좌우 여백 데스크톱 `64px` / 모바일 `20px`
- 섹션 상하 패딩: 데스크톱 `160px`, 태블릿 `112px`, 모바일 `72px`
- 스페이싱 스케일: `4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96 / 128 / 160`
- **비대칭 배치를 기본으로 한다.** 좌측 5컬럼 텍스트 + 우측 6컬럼 데이터, 1컬럼 공백 — 중앙 정렬 3단 카드 그리드의 반복을 피한다.
- 구분은 **1px hairline**으로만. 라이트 섹션에 그림자 사용 금지.
- 라운드: 버튼 `999px`(pill), 패널·카드 `12px`, 입력 필드 `8px`
- 그림자: 상단 플로팅 내비게이션 **한 곳만** `0 1px 2px rgba(14,14,16,.04), 0 8px 24px rgba(14,14,16,.06)`

---

## 6. 컴포넌트 규격

### 내비게이션
화면 상단에서 24px 떠 있는 **플로팅 pill 바**. 초기엔 배경 투명, 스크롤 8px 이후 `paper` 배경 + hairline + 위 그림자로 전환. 좌측에 `K Select Network` 타입 워드마크, 중앙에 메뉴, 우측에 언어 토글(KR / EN, 텍스트만)과 주 CTA pill("파트너 신청"). 다크 히어로 위에서는 내비 텍스트가 `ivory`, 라이트 섹션 진입 시 `graphite`로 전환.

### 푸터
`ink` 배경. 좌측에 워드마크 + `Operated by Letusto Inc.` 1줄. 중앙에 사이트맵 3열. 우측에 담당자 이메일·전화. 최하단 hairline-dark 위에 `© Letusto Inc.` 및 개인정보처리방침. 푸터에서만 세 이름(K Select Network / K-Beauty Growth Program / Letusto Inc.)이 함께 등장한다.

### 버튼
- Primary: `accent` 배경 / `#FFF` 텍스트 / pill / 패딩 `16px 32px` / 라벨은 15px 600
- Secondary: 배경 없음 / 1px `hairline` 보더 / `graphite` 텍스트
- Tertiary: 텍스트 + 우측 화살표 `↗`, hover 시 화살표만 2px 이동
- hover는 색 변화 + `translateY(-1px)`. 크기 확대(scale) 금지.

### 지표 블록 (Stat)
숫자 `stat` 사이즈 800 + 아래 `label`. 숫자는 `font-variant-numeric: tabular-nums`. 카드 배경 없이 hairline으로만 구획. 뷰포트 진입 시 count-up.

### 절차 레일 (Process Rail)
9단계를 카드 그리드가 아니라 **수평 레일**로. 각 스텝은 `accent` 색 2자리 번호(`01`…`09`) + 제목 + 1줄 설명. 1단계(테스트)와 2단계(본 파트너십)를 hairline과 단계 라벨로 구분. 데스크톱은 가로 스크롤 레일, 모바일은 좌측 세로 타임라인.

### 문제 → 해법 대조 (Contrast Block)
좌측 다크(`ink-raised`)에 "중소 브랜드가 부딪히는 벽" 5개, 우측 라이트에 "Letusto의 해법" 4개를 좌우로 붙여 배치. 이 대조가 사이트에서 가장 강한 시각적 순간이다. 스크롤에 따라 우측 항목이 순차 등장.

### 자가진단 (Eligibility Check)
6개 조건을 한 화면 세로 리스트로. 각 항목은 토글 스위치가 아니라 **큰 체크 카드**(전체 영역 클릭). 선택 시 좌측 4px `accent` 바가 나타난다. 하단에 고정 진행 표시("4 / 6 충족"). 6개 모두 충족 시 CTA가 활성화되며 `accent`로 채워진다.

### 신청폼
1열 세로. 라벨은 필드 위 `label` 스타일. focus 시 2px `accent` ring. 섹션(브랜드 정보 / 상품 정보 / 생산 역량 / 담당자)마다 hairline과 로마숫자 넘버링. 진행률 바 상단 고정.

### 데이터 바 차트 (카테고리 비중)
가로 바. 활성 바만 `accent`, 나머지는 `slate` 40%. 값 라벨은 우측 정렬 tabular-nums. 차트 라이브러리 없이 순수 CSS/SVG.

---

## 7. 이미지 및 비주얼

**원칙: 스톡 사진을 쓰지 않는다.** 모든 비주얼은 계산된 도형이다.

- 미국 매장 네트워크 → 인라인 SVG 지도. 거점은 `accent` 점, 확장 예정 지역은 hairline 링.
- 카테고리 비중 → CSS 바 차트
- 절차 → 넘버링 레일
- 시장 성장 → 단순 라인/에어리어 SVG. 그리드선은 hairline.
- 배경 텍스처 → 아주 옅은 SVG 그리드(`#0E0E10` 위 `#FFFFFF` 3% opacity, 1px, 48px 간격)를 다크 히어로에만.

**실사진 슬롯 (자산 확보 시 교체):** Beauty Supply 매장 외관, Store-in-a-Store 진열 실물, 제품 진열대 클로즈업. 이 3개 위치만 사진을 받는다. 확보 전까지는 위 SVG 비주얼로 대체하고, 자리를 비워두거나 플레이스홀더 회색 박스를 넣지 않는다.

절대 쓰지 않는 이미지: 웃는 모델 얼굴 스톡, 악수 사진, 지구본/화살표 클립아트, AI 생성 화장품 렌더, 이모지 아이콘.

---

## 8. 모션

- 트리거: 뷰포트 진입 시 1회. 무한 루프 애니메이션 없음.
- 값: `opacity 0→1`, `translateY 16px→0`. transform과 opacity만 사용.
- 타이밍: `500ms cubic-bezier(0.16, 1, 0.3, 1)`
- 스태거: 리스트 항목당 60ms
- 지표 count-up: 1200ms, ease-out
- 문제→해법 대조 블록은 스크롤 스크럽(scroll-linked)으로 우측 항목 순차 노출
- `prefers-reduced-motion: reduce`에서 전부 즉시 표시로 폴백
- 패럴랙스, 커서 트레일, 3D 틸트, 타이핑 효과 금지

---

## 9. 안티슬롭 목록

다음은 이 프로젝트에서 **결함**으로 취급한다.

- Inter / Roboto 폰트
- 보라·바이올렛 그라디언트, 무지개 그라디언트, 메시 그라디언트
- 글래스모피즘, 네온 글로우, 굵은 드롭섀도
- 동일한 아이콘 카드 3개가 중앙 정렬로 놓인 그리드
- 이모지를 아이콘으로 사용
- 흐릿하게 처리한 "Trusted by" 로고 행
- 그라디언트로 채운 텍스트
- 전체 폭 히어로 배경 사진 + 그 위 흰 글씨
- 다크 모드 토글 (이 사이트는 다크/라이트를 섹션 구조로 이미 쓴다)
- 라운드 24px 이상의 물렁한 카드
- 액센트 색을 3회 이상 쓴 화면

---

## 10. 페이지 목록

| 경로 | 페이지 | 배경 구조 |
|---|---|---|
| `/` | 홈 | 다크 히어로 → 라이트 본문 → 다크 CTA |
| `/program` | 프로그램 개요 · 왜 지금 미국인가 · 소싱 카테고리 | 라이트 |
| `/why-letusto` | 회사 소개 · 유통 네트워크 · 데이터 기반 파트너십 | 라이트 + 지표 다크 밴드 |
| `/benefits` | 부딪히는 벽 → Letusto의 해법 → 구체적 가치 | 다크/라이트 대조 |
| `/process` | 파트너십 절차 (테스트 → 본 파트너십) | 라이트 |
| `/eligibility` | 참여 자격 6조건 + 자가진단 진입 | 라이트 |
| `/resources` | FAQ · 소개서 다운로드 | 라이트 |
| `/contact` | 담당자 연락처 · 일반 문의 | 라이트 |
| `/apply/check` | 자가진단 6문항 | 라이트 (집중 모드, 내비 축소) |
| `/apply/form` | 정식 신청폼 | 라이트 (집중 모드) |
| `/apply/meeting` | 미팅 예약 (Microsoft Bookings 임베드) | 라이트 |
| `/apply/done` | 접수 완료 | 다크 |

---

## 11. 사실 자산 (디자인에 실제로 들어갈 콘텐츠)

문구를 창작하지 말고 아래 값을 사용한다.

**회사:** Letusto Inc. · 설립 2004 · 누적 판매 US$250M+ · 운영 브랜드 10+ · 거점 3개국(미·한·중)
**플랫폼(사이트 브랜드):** K Select Network — 이 사이트의 이름이자 웹 주소
**실행 프로그램(홍보 대상):** K-Beauty Growth Program
> 참고: 소개서 PDF에는 유통 플랫폼이 "K-Beauty Retail Network"로 표기되어 있다. 사이트 브랜드는 **K Select Network**로 확정되었으므로, 자료와 사이트의 명칭을 통일할지 여부를 확인해야 한다. (미해결 항목)
**시장:** 2025 미국 K-Beauty 시장 $27.6B · CAGR 9.5% (2026–2033) · 글로벌 점유율 23.4% · 2025 대미 수출 $2.2B · 스킨케어가 2025년 기준 68.7% 비중
**카테고리 비중:** K-스킨케어 75% / K-헤어&스칼프 10% / K-뷰티 소품·툴 5% / K-데일리 케어 5% / K-웰니스·기능성 패치 5%
**차별 정책 (고유 명칭이므로 그대로 표기):** Store-in-a-Store 진열 · 상권 보호 정책 · First Order Protection · 주간/월간 실판매 데이터 검증
**테스트 규모:** 오프라인 10개 매장 240개 + 온라인(Amazon) 60개
**담당:** Chae Hahm (함채환) · chae@letusto.com · 856-383-8288 (한국 지사 정보 미정)

**핵심 카피 (변경 금지):**
- 히어로: "낯설고 두려운 미국 진출이 아니라, 검증된 파트너와 함께 시작하는 성장입니다."
- "브랜드는 상품에, Letusto는 미국 현지 운영에 — 역할을 나누어 함께 성장합니다."
- "감이 아니라 데이터로 함께 성장합니다."

---

## 12. 기술 스택 (Claude Code 단계)

Next.js (App Router) · TypeScript · Tailwind CSS · 디자인 토큰은 CSS 변수로 선언 후 Tailwind theme에 매핑 · Pretendard Variable + Instrument Serif는 self-host 또는 CDN · Microsoft Bookings iframe 임베드 · Git → Vercel 배포.
